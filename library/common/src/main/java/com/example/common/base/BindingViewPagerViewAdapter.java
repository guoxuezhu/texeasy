package com.example.common.base;

import androidx.databinding.ViewDataBinding;

public class BindingViewPagerViewAdapter<ItemViewModel> extends me.tatarka.bindingcollectionadapter2.BindingViewPagerAdapter {
    @Override
    public void onBindBinding(ViewDataBinding binding, int variableId, int layoutRes, int position, Object item) {
        super.onBindBinding(binding, variableId, layoutRes, position, item);
    }
}
